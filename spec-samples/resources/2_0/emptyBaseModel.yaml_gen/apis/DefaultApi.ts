/**
 * Test Command model generation
 * Test Command model generation
 *
 * The version of the OpenAPI document: 1.0.0
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit the class manually.
 */

import type {
  UrlQueryInput,
  OperationConfiguration,
  RequestConfiguration,
  ResponseConfiguration,
  FetchParameters,
} from '@rocket-scripts/openapi';
import {
  pipe,
  collectionFormats,
  querystring,
  fetchRequest,
  APIRequestError,
  APIExceptionError,
} from '@rocket-scripts/openapi';
import type { ApiError, RealCommand } from '../models';
import { typeSerializer } from '../runtime';

/* eslint-disable */

export interface ExecuteRealCommandRequestParameters {
  realCommand: RealCommand;
}

/**
 * @throws {APIRequestError} throw when the request is not correct
 * @throws {APISerializeError} throw when fail serialize data (include request and response)
 */
export const executeRealCommandRequest = ({
  basePath = '',
  querystringStringify = querystring,
  username,
  password,
  apiKey,
  accessToken,
  headers,
  credentials,
}: RequestConfiguration = {}) => (
  requestParameters: ExecuteRealCommandRequestParameters,
): FetchParameters => {
  if (
    requestParameters.realCommand === null ||
    requestParameters.realCommand === undefined
  ) {
    throw new APIRequestError(
      `Required parameter requestParameters.realCommand was null or undefined when calling executeRealCommand.`,
    );
  }

  const queryParameters: UrlQueryInput = {};

  const headerParameters: Headers = new Headers(headers);

  headerParameters.set('Content-Type', 'application/json');

  let url: string = `${basePath.replace(/\/+$/, '')}/execute`;
  if (queryParameters && Object.keys(queryParameters).length > 0) {
    url += '?' + querystringStringify(queryParameters);
  }

  const body = JSON.stringify(
    typeSerializer.toJsonObject('RealCommand')(requestParameters.realCommand),
  );

  return {
    input: url,
    init: {
      method: 'POST',
      headers: headerParameters,
      credentials,
      body,
    },
  };
};

/**
 * @throws {APIExceptionError} throw when response is out of 2xx
 * @throws {Response} throw when the response.status is not accepted (fallback throwing)
 * @throws {APISerializeError} throw when fail serialize data (include request and response)
 */
export const executeRealCommandResponse = (
  configuration: ResponseConfiguration = {},
) => async (response: Response): Promise<void> => {
  if (response.status >= 200 && response.status < 300) {
  } else {
    switch (response.status.toString()) {
      case '400': // Bad request.
        throw new APIExceptionError({
          exception: typeSerializer.toValueObject('ApiError')(
            await response.json(),
          ),
          message: `Bad request.`,
          response,
        });
      case '404': // Not found.
        throw new APIExceptionError({
          exception: typeSerializer.toValueObject('ApiError')(
            await response.json(),
          ),
          message: `Not found.`,
          response,
        });
      case '0': // Unknown error.
        throw new APIExceptionError({
          exception: typeSerializer.toValueObject('ApiError')(
            await response.json(),
          ),
          message: `Unknown error.`,
          response,
        });
      default:
        throw response;
    }
  }
};

/**
 * @throws {APIRequestError} throw when the request is not correct
 * @throws {APIExceptionError} throw when response is out of 2xx
 * @throws {Response} throw when the response.status is not accepted (fallback throwing)
 * @throws {APISerializeError} throw when fail serialize data (include request and response)
 */
export async function executeRealCommand(
  requestParameters: ExecuteRealCommandRequestParameters,
  configuration: OperationConfiguration = {},
): Promise<void> {
  return pipe(
    executeRealCommandRequest(configuration),
    fetchRequest(configuration),
    executeRealCommandResponse(configuration),
  )(requestParameters);
}

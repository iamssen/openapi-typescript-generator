/**
 * The Wordnik Public API
 * **Wordnik has an API, and you\'re invited.** The Wordnik API lets you request definitions, example sentences, spelling suggestions,  related words like synonyms and antonyms, phrases containing a given word, word  autocompletion, random words, words of the day, and much more
 *
 * The version of the OpenAPI document: 4.0.1
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit the class manually.
 */

import type {
  UrlQueryInput,
  OperationConfiguration,
  RequestConfiguration,
  ResponseConfiguration,
  FetchParameters,
} from '@rocket-scripts/openapi';
import {
  pipe,
  collectionFormats,
  querystring,
  fetchRequest,
  APIRequestError,
  APIExceptionError,
} from '@rocket-scripts/openapi';
import type { StringValue, WordList, WordListWord } from '../models';
import { typeSerializer } from '../runtime';

/* eslint-disable */

export interface AddWordsToWordListRequestParameters {
  permalink: string;
  authToken: string;
  body?: Array<StringValue>;
}
export interface DeleteWordListRequestParameters {
  permalink: string;
  authToken: string;
}
export interface DeleteWordsFromWordListRequestParameters {
  permalink: string;
  authToken: string;
  body?: Array<StringValue>;
}
export interface GetWordListByPermalinkRequestParameters {
  permalink: string;
  authToken: string;
}
export interface GetWordListWordsRequestParameters {
  permalink: string;
  authToken: string;
  sortBy?: string;
  sortOrder?: string;
  skip?: number;
  limit?: number;
}
export interface UpdateWordListRequestParameters {
  permalink: string;
  authToken: string;
  body?: WordList;
}

/**
 * Adds words to a WordList
 * @throws {APIRequestError} throw when the request is not correct
 * @throws {APISerializeError} throw when fail serialize data (include request and response)
 */
export const addWordsToWordListRequest = ({
  basePath = '',
  querystringStringify = querystring,
  username,
  password,
  apiKey,
  accessToken,
  headers,
  credentials,
}: RequestConfiguration = {}) => (
  requestParameters: AddWordsToWordListRequestParameters,
): FetchParameters => {
  if (
    requestParameters.permalink === null ||
    requestParameters.permalink === undefined
  ) {
    throw new APIRequestError(
      `Required parameter requestParameters.permalink was null or undefined when calling addWordsToWordList.`,
    );
  }
  if (
    requestParameters.authToken === null ||
    requestParameters.authToken === undefined
  ) {
    throw new APIRequestError(
      `Required parameter requestParameters.authToken was null or undefined when calling addWordsToWordList.`,
    );
  }

  const queryParameters: UrlQueryInput = {};

  const headerParameters: Headers = new Headers(headers);

  headerParameters.set('Content-Type', 'application/json');

  if (
    requestParameters.authToken !== undefined &&
    requestParameters.authToken !== null
  ) {
    headerParameters.set('auth_token', String(requestParameters.authToken));
  }

  if (apiKey) {
    headerParameters.set('api_key', apiKey('api_key')); // apiKey authentication
  }

  let url: string = `${basePath.replace(
    /\/+$/,
    '',
  )}/wordList.json/{permalink}/words`.replace(
    `{${'permalink'}}`,
    encodeURIComponent(String(requestParameters.permalink)),
  );
  if (queryParameters && Object.keys(queryParameters).length > 0) {
    url += '?' + querystringStringify(queryParameters);
  }

  const body = JSON.stringify(
    requestParameters.body?.map(typeSerializer.toJsonObject('StringValue')),
  );

  return {
    input: url,
    init: {
      method: 'POST',
      headers: headerParameters,
      credentials,
      body,
    },
  };
};

/**
 * Adds words to a WordList
 * @throws {APIExceptionError} throw when response is out of 2xx
 * @throws {Response} throw when the response.status is not accepted (fallback throwing)
 * @throws {APISerializeError} throw when fail serialize data (include request and response)
 */
export const addWordsToWordListResponse = (
  configuration: ResponseConfiguration = {},
) => async (response: Response): Promise<void> => {
  if (response.status >= 200 && response.status < 300) {
  } else {
    switch (response.status.toString()) {
      default:
        throw response;
    }
  }
};

/**
 * Adds words to a WordList
 * @throws {APIRequestError} throw when the request is not correct
 * @throws {APIExceptionError} throw when response is out of 2xx
 * @throws {Response} throw when the response.status is not accepted (fallback throwing)
 * @throws {APISerializeError} throw when fail serialize data (include request and response)
 */
export async function addWordsToWordList(
  requestParameters: AddWordsToWordListRequestParameters,
  configuration: OperationConfiguration = {},
): Promise<void> {
  return pipe(
    addWordsToWordListRequest(configuration),
    fetchRequest(configuration),
    addWordsToWordListResponse(configuration),
  )(requestParameters);
}

/**
 * Deletes an existing WordList
 * @throws {APIRequestError} throw when the request is not correct
 * @throws {APISerializeError} throw when fail serialize data (include request and response)
 */
export const deleteWordListRequest = ({
  basePath = '',
  querystringStringify = querystring,
  username,
  password,
  apiKey,
  accessToken,
  headers,
  credentials,
}: RequestConfiguration = {}) => (
  requestParameters: DeleteWordListRequestParameters,
): FetchParameters => {
  if (
    requestParameters.permalink === null ||
    requestParameters.permalink === undefined
  ) {
    throw new APIRequestError(
      `Required parameter requestParameters.permalink was null or undefined when calling deleteWordList.`,
    );
  }
  if (
    requestParameters.authToken === null ||
    requestParameters.authToken === undefined
  ) {
    throw new APIRequestError(
      `Required parameter requestParameters.authToken was null or undefined when calling deleteWordList.`,
    );
  }

  const queryParameters: UrlQueryInput = {};

  const headerParameters: Headers = new Headers(headers);

  if (
    requestParameters.authToken !== undefined &&
    requestParameters.authToken !== null
  ) {
    headerParameters.set('auth_token', String(requestParameters.authToken));
  }

  if (apiKey) {
    headerParameters.set('api_key', apiKey('api_key')); // apiKey authentication
  }

  let url: string = `${basePath.replace(
    /\/+$/,
    '',
  )}/wordList.json/{permalink}`.replace(
    `{${'permalink'}}`,
    encodeURIComponent(String(requestParameters.permalink)),
  );
  if (queryParameters && Object.keys(queryParameters).length > 0) {
    url += '?' + querystringStringify(queryParameters);
  }

  const body = undefined;

  return {
    input: url,
    init: {
      method: 'DELETE',
      headers: headerParameters,
      credentials,
      body,
    },
  };
};

/**
 * Deletes an existing WordList
 * @throws {APIExceptionError} throw when response is out of 2xx
 * @throws {Response} throw when the response.status is not accepted (fallback throwing)
 * @throws {APISerializeError} throw when fail serialize data (include request and response)
 */
export const deleteWordListResponse = (
  configuration: ResponseConfiguration = {},
) => async (response: Response): Promise<void> => {
  if (response.status >= 200 && response.status < 300) {
  } else {
    switch (response.status.toString()) {
      default:
        throw response;
    }
  }
};

/**
 * Deletes an existing WordList
 * @throws {APIRequestError} throw when the request is not correct
 * @throws {APIExceptionError} throw when response is out of 2xx
 * @throws {Response} throw when the response.status is not accepted (fallback throwing)
 * @throws {APISerializeError} throw when fail serialize data (include request and response)
 */
export async function deleteWordList(
  requestParameters: DeleteWordListRequestParameters,
  configuration: OperationConfiguration = {},
): Promise<void> {
  return pipe(
    deleteWordListRequest(configuration),
    fetchRequest(configuration),
    deleteWordListResponse(configuration),
  )(requestParameters);
}

/**
 * Removes words from a WordList
 * @throws {APIRequestError} throw when the request is not correct
 * @throws {APISerializeError} throw when fail serialize data (include request and response)
 */
export const deleteWordsFromWordListRequest = ({
  basePath = '',
  querystringStringify = querystring,
  username,
  password,
  apiKey,
  accessToken,
  headers,
  credentials,
}: RequestConfiguration = {}) => (
  requestParameters: DeleteWordsFromWordListRequestParameters,
): FetchParameters => {
  if (
    requestParameters.permalink === null ||
    requestParameters.permalink === undefined
  ) {
    throw new APIRequestError(
      `Required parameter requestParameters.permalink was null or undefined when calling deleteWordsFromWordList.`,
    );
  }
  if (
    requestParameters.authToken === null ||
    requestParameters.authToken === undefined
  ) {
    throw new APIRequestError(
      `Required parameter requestParameters.authToken was null or undefined when calling deleteWordsFromWordList.`,
    );
  }

  const queryParameters: UrlQueryInput = {};

  const headerParameters: Headers = new Headers(headers);

  headerParameters.set('Content-Type', 'application/json');

  if (
    requestParameters.authToken !== undefined &&
    requestParameters.authToken !== null
  ) {
    headerParameters.set('auth_token', String(requestParameters.authToken));
  }

  if (apiKey) {
    headerParameters.set('api_key', apiKey('api_key')); // apiKey authentication
  }

  let url: string = `${basePath.replace(
    /\/+$/,
    '',
  )}/wordList.json/{permalink}/deleteWords`.replace(
    `{${'permalink'}}`,
    encodeURIComponent(String(requestParameters.permalink)),
  );
  if (queryParameters && Object.keys(queryParameters).length > 0) {
    url += '?' + querystringStringify(queryParameters);
  }

  const body = JSON.stringify(
    requestParameters.body?.map(typeSerializer.toJsonObject('StringValue')),
  );

  return {
    input: url,
    init: {
      method: 'POST',
      headers: headerParameters,
      credentials,
      body,
    },
  };
};

/**
 * Removes words from a WordList
 * @throws {APIExceptionError} throw when response is out of 2xx
 * @throws {Response} throw when the response.status is not accepted (fallback throwing)
 * @throws {APISerializeError} throw when fail serialize data (include request and response)
 */
export const deleteWordsFromWordListResponse = (
  configuration: ResponseConfiguration = {},
) => async (response: Response): Promise<void> => {
  if (response.status >= 200 && response.status < 300) {
  } else {
    switch (response.status.toString()) {
      default:
        throw response;
    }
  }
};

/**
 * Removes words from a WordList
 * @throws {APIRequestError} throw when the request is not correct
 * @throws {APIExceptionError} throw when response is out of 2xx
 * @throws {Response} throw when the response.status is not accepted (fallback throwing)
 * @throws {APISerializeError} throw when fail serialize data (include request and response)
 */
export async function deleteWordsFromWordList(
  requestParameters: DeleteWordsFromWordListRequestParameters,
  configuration: OperationConfiguration = {},
): Promise<void> {
  return pipe(
    deleteWordsFromWordListRequest(configuration),
    fetchRequest(configuration),
    deleteWordsFromWordListResponse(configuration),
  )(requestParameters);
}

/**
 * Fetches a WordList by ID
 * @throws {APIRequestError} throw when the request is not correct
 * @throws {APISerializeError} throw when fail serialize data (include request and response)
 */
export const getWordListByPermalinkRequest = ({
  basePath = '',
  querystringStringify = querystring,
  username,
  password,
  apiKey,
  accessToken,
  headers,
  credentials,
}: RequestConfiguration = {}) => (
  requestParameters: GetWordListByPermalinkRequestParameters,
): FetchParameters => {
  if (
    requestParameters.permalink === null ||
    requestParameters.permalink === undefined
  ) {
    throw new APIRequestError(
      `Required parameter requestParameters.permalink was null or undefined when calling getWordListByPermalink.`,
    );
  }
  if (
    requestParameters.authToken === null ||
    requestParameters.authToken === undefined
  ) {
    throw new APIRequestError(
      `Required parameter requestParameters.authToken was null or undefined when calling getWordListByPermalink.`,
    );
  }

  const queryParameters: UrlQueryInput = {};

  const headerParameters: Headers = new Headers(headers);

  if (
    requestParameters.authToken !== undefined &&
    requestParameters.authToken !== null
  ) {
    headerParameters.set('auth_token', String(requestParameters.authToken));
  }

  if (apiKey) {
    headerParameters.set('api_key', apiKey('api_key')); // apiKey authentication
  }

  let url: string = `${basePath.replace(
    /\/+$/,
    '',
  )}/wordList.json/{permalink}`.replace(
    `{${'permalink'}}`,
    encodeURIComponent(String(requestParameters.permalink)),
  );
  if (queryParameters && Object.keys(queryParameters).length > 0) {
    url += '?' + querystringStringify(queryParameters);
  }

  const body = undefined;

  return {
    input: url,
    init: {
      method: 'GET',
      headers: headerParameters,
      credentials,
      body,
    },
  };
};

/**
 * Fetches a WordList by ID
 * @throws {APIExceptionError} throw when response is out of 2xx
 * @throws {Response} throw when the response.status is not accepted (fallback throwing)
 * @throws {APISerializeError} throw when fail serialize data (include request and response)
 */
export const getWordListByPermalinkResponse = (
  configuration: ResponseConfiguration = {},
) => async (response: Response): Promise<WordList> => {
  if (response.status >= 200 && response.status < 300) {
    return response.json().then(typeSerializer.toValueObject('WordList'));
  } else {
    switch (response.status.toString()) {
      default:
        throw response;
    }
  }
};

/**
 * Fetches a WordList by ID
 * @throws {APIRequestError} throw when the request is not correct
 * @throws {APIExceptionError} throw when response is out of 2xx
 * @throws {Response} throw when the response.status is not accepted (fallback throwing)
 * @throws {APISerializeError} throw when fail serialize data (include request and response)
 */
export async function getWordListByPermalink(
  requestParameters: GetWordListByPermalinkRequestParameters,
  configuration: OperationConfiguration = {},
): Promise<WordList> {
  return pipe(
    getWordListByPermalinkRequest(configuration),
    fetchRequest(configuration),
    getWordListByPermalinkResponse(configuration),
  )(requestParameters);
}

/**
 * Fetches words in a WordList
 * @throws {APIRequestError} throw when the request is not correct
 * @throws {APISerializeError} throw when fail serialize data (include request and response)
 */
export const getWordListWordsRequest = ({
  basePath = '',
  querystringStringify = querystring,
  username,
  password,
  apiKey,
  accessToken,
  headers,
  credentials,
}: RequestConfiguration = {}) => (
  requestParameters: GetWordListWordsRequestParameters,
): FetchParameters => {
  if (
    requestParameters.permalink === null ||
    requestParameters.permalink === undefined
  ) {
    throw new APIRequestError(
      `Required parameter requestParameters.permalink was null or undefined when calling getWordListWords.`,
    );
  }
  if (
    requestParameters.authToken === null ||
    requestParameters.authToken === undefined
  ) {
    throw new APIRequestError(
      `Required parameter requestParameters.authToken was null or undefined when calling getWordListWords.`,
    );
  }

  const queryParameters: UrlQueryInput = {};

  if (requestParameters.sortBy !== undefined) {
    queryParameters['sortBy'] = requestParameters.sortBy;
  }
  if (requestParameters.sortOrder !== undefined) {
    queryParameters['sortOrder'] = requestParameters.sortOrder;
  }
  if (requestParameters.skip !== undefined) {
    queryParameters['skip'] = requestParameters.skip;
  }
  if (requestParameters.limit !== undefined) {
    queryParameters['limit'] = requestParameters.limit;
  }

  const headerParameters: Headers = new Headers(headers);

  if (
    requestParameters.authToken !== undefined &&
    requestParameters.authToken !== null
  ) {
    headerParameters.set('auth_token', String(requestParameters.authToken));
  }

  if (apiKey) {
    headerParameters.set('api_key', apiKey('api_key')); // apiKey authentication
  }

  let url: string = `${basePath.replace(
    /\/+$/,
    '',
  )}/wordList.json/{permalink}/words`.replace(
    `{${'permalink'}}`,
    encodeURIComponent(String(requestParameters.permalink)),
  );
  if (queryParameters && Object.keys(queryParameters).length > 0) {
    url += '?' + querystringStringify(queryParameters);
  }

  const body = undefined;

  return {
    input: url,
    init: {
      method: 'GET',
      headers: headerParameters,
      credentials,
      body,
    },
  };
};

/**
 * Fetches words in a WordList
 * @throws {APIExceptionError} throw when response is out of 2xx
 * @throws {Response} throw when the response.status is not accepted (fallback throwing)
 * @throws {APISerializeError} throw when fail serialize data (include request and response)
 */
export const getWordListWordsResponse = (
  configuration: ResponseConfiguration = {},
) => async (response: Response): Promise<Array<WordListWord>> => {
  if (response.status >= 200 && response.status < 300) {
    return response
      .json()
      .then(typeSerializer.toValueObjectList('WordListWord'));
  } else {
    switch (response.status.toString()) {
      default:
        throw response;
    }
  }
};

/**
 * Fetches words in a WordList
 * @throws {APIRequestError} throw when the request is not correct
 * @throws {APIExceptionError} throw when response is out of 2xx
 * @throws {Response} throw when the response.status is not accepted (fallback throwing)
 * @throws {APISerializeError} throw when fail serialize data (include request and response)
 */
export async function getWordListWords(
  requestParameters: GetWordListWordsRequestParameters,
  configuration: OperationConfiguration = {},
): Promise<Array<WordListWord>> {
  return pipe(
    getWordListWordsRequest(configuration),
    fetchRequest(configuration),
    getWordListWordsResponse(configuration),
  )(requestParameters);
}

/**
 * Updates an existing WordList
 * @throws {APIRequestError} throw when the request is not correct
 * @throws {APISerializeError} throw when fail serialize data (include request and response)
 */
export const updateWordListRequest = ({
  basePath = '',
  querystringStringify = querystring,
  username,
  password,
  apiKey,
  accessToken,
  headers,
  credentials,
}: RequestConfiguration = {}) => (
  requestParameters: UpdateWordListRequestParameters,
): FetchParameters => {
  if (
    requestParameters.permalink === null ||
    requestParameters.permalink === undefined
  ) {
    throw new APIRequestError(
      `Required parameter requestParameters.permalink was null or undefined when calling updateWordList.`,
    );
  }
  if (
    requestParameters.authToken === null ||
    requestParameters.authToken === undefined
  ) {
    throw new APIRequestError(
      `Required parameter requestParameters.authToken was null or undefined when calling updateWordList.`,
    );
  }

  const queryParameters: UrlQueryInput = {};

  const headerParameters: Headers = new Headers(headers);

  headerParameters.set('Content-Type', 'application/json');

  if (
    requestParameters.authToken !== undefined &&
    requestParameters.authToken !== null
  ) {
    headerParameters.set('auth_token', String(requestParameters.authToken));
  }

  if (apiKey) {
    headerParameters.set('api_key', apiKey('api_key')); // apiKey authentication
  }

  let url: string = `${basePath.replace(
    /\/+$/,
    '',
  )}/wordList.json/{permalink}`.replace(
    `{${'permalink'}}`,
    encodeURIComponent(String(requestParameters.permalink)),
  );
  if (queryParameters && Object.keys(queryParameters).length > 0) {
    url += '?' + querystringStringify(queryParameters);
  }

  const body = JSON.stringify(
    typeSerializer.toJsonObject('WordList')(requestParameters.body),
  );

  return {
    input: url,
    init: {
      method: 'PUT',
      headers: headerParameters,
      credentials,
      body,
    },
  };
};

/**
 * Updates an existing WordList
 * @throws {APIExceptionError} throw when response is out of 2xx
 * @throws {Response} throw when the response.status is not accepted (fallback throwing)
 * @throws {APISerializeError} throw when fail serialize data (include request and response)
 */
export const updateWordListResponse = (
  configuration: ResponseConfiguration = {},
) => async (response: Response): Promise<void> => {
  if (response.status >= 200 && response.status < 300) {
  } else {
    switch (response.status.toString()) {
      default:
        throw response;
    }
  }
};

/**
 * Updates an existing WordList
 * @throws {APIRequestError} throw when the request is not correct
 * @throws {APIExceptionError} throw when response is out of 2xx
 * @throws {Response} throw when the response.status is not accepted (fallback throwing)
 * @throws {APISerializeError} throw when fail serialize data (include request and response)
 */
export async function updateWordList(
  requestParameters: UpdateWordListRequestParameters,
  configuration: OperationConfiguration = {},
): Promise<void> {
  return pipe(
    updateWordListRequest(configuration),
    fetchRequest(configuration),
    updateWordListResponse(configuration),
  )(requestParameters);
}

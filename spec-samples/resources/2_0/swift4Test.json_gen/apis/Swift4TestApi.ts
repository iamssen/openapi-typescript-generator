/**
 * Swift 4 Test Schema
 * This is a test schema which exercises Swagger schema features for testing the swift4 language codegen module.
 *
 * The version of the OpenAPI document: 1.0
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit the class manually.
 */

import type {
  UrlQueryInput,
  OperationConfiguration,
  RequestConfiguration,
  ResponseConfiguration,
  FetchParameters,
} from '@rocket-scripts/openapi';
import {
  pipe,
  collectionFormats,
  querystring,
  fetchRequest,
  APIRequestError,
  APIExceptionError,
} from '@rocket-scripts/openapi';
import type { ErrorInfo, GetAllModelsResult } from '../models';
import { typeSerializer } from '../runtime';

/* eslint-disable */

export interface GetAllModelsRequestParameters {
  clientId: string;
}

/**
 * This endpoint tests get a dictionary which contains examples of all of the models.
 * Get all of the models
 * @throws {APIRequestError} throw when the request is not correct
 * @throws {APISerializeError} throw when fail serialize data (include request and response)
 */
export const getAllModelsRequest = ({
  basePath = '',
  querystringStringify = querystring,
  username,
  password,
  apiKey,
  accessToken,
  headers,
  credentials,
}: RequestConfiguration = {}) => (
  requestParameters: GetAllModelsRequestParameters,
): FetchParameters => {
  if (
    requestParameters.clientId === null ||
    requestParameters.clientId === undefined
  ) {
    throw new APIRequestError(
      `Required parameter requestParameters.clientId was null or undefined when calling getAllModels.`,
    );
  }

  const queryParameters: UrlQueryInput = {};

  if (requestParameters.clientId !== undefined) {
    queryParameters['client_id'] = requestParameters.clientId;
  }

  const headerParameters: Headers = new Headers(headers);

  let url: string = `${basePath.replace(/\/+$/, '')}/allModels`;
  if (queryParameters && Object.keys(queryParameters).length > 0) {
    url += '?' + querystringStringify(queryParameters);
  }

  const body = undefined;

  return {
    input: url,
    init: {
      method: 'GET',
      headers: headerParameters,
      credentials,
      body,
    },
  };
};

/**
 * This endpoint tests get a dictionary which contains examples of all of the models.
 * Get all of the models
 * @throws {APIExceptionError} throw when response is out of 2xx
 * @throws {Response} throw when the response.status is not accepted (fallback throwing)
 * @throws {APISerializeError} throw when fail serialize data (include request and response)
 */
export const getAllModelsResponse = (
  configuration: ResponseConfiguration = {},
) => async (response: Response): Promise<GetAllModelsResult> => {
  if (response.status >= 200 && response.status < 300) {
    return response
      .json()
      .then(typeSerializer.toValueObject('GetAllModelsResult'));
  } else {
    switch (response.status.toString()) {
      case '400': // Invalid client input
        throw new APIExceptionError({
          exception: typeSerializer.toValueObject('ErrorInfo')(
            await response.json(),
          ),
          message: `Invalid client input`,
          response,
        });
      case '424': // Timeout
        throw new APIExceptionError({
          exception: typeSerializer.toValueObject('ErrorInfo')(
            await response.json(),
          ),
          message: `Timeout`,
          response,
        });
      case '500': // Unexpected Server Error
        throw new APIExceptionError({
          exception: typeSerializer.toValueObject('ErrorInfo')(
            await response.json(),
          ),
          message: `Unexpected Server Error`,
          response,
        });
      default:
        throw response;
    }
  }
};

/**
 * This endpoint tests get a dictionary which contains examples of all of the models.
 * Get all of the models
 * @throws {APIRequestError} throw when the request is not correct
 * @throws {APIExceptionError} throw when response is out of 2xx
 * @throws {Response} throw when the response.status is not accepted (fallback throwing)
 * @throws {APISerializeError} throw when fail serialize data (include request and response)
 */
export async function getAllModels(
  requestParameters: GetAllModelsRequestParameters,
  configuration: OperationConfiguration = {},
): Promise<GetAllModelsResult> {
  return pipe(
    getAllModelsRequest(configuration),
    fetchRequest(configuration),
    getAllModelsResponse(configuration),
  )(requestParameters);
}

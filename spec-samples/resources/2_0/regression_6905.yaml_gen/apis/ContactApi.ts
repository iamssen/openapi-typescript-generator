/**
 * Test Command model generation
 * Test Command model generation
 *
 * The version of the OpenAPI document: 1.0.0
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit the class manually.
 */

import type {
  UrlQueryInput,
  OperationConfiguration,
  RequestConfiguration,
  ResponseConfiguration,
  FetchParameters,
} from '@rocket-scripts/openapi';
import {
  pipe,
  collectionFormats,
  querystring,
  fetchRequest,
  APIRequestError,
  APIExceptionError,
} from '@rocket-scripts/openapi';
import type { ApiError, Contact } from '../models';
import { typeSerializer } from '../runtime';

/* eslint-disable */

/**
 * Get contact
 * @throws {APIRequestError} throw when the request is not correct
 * @throws {APISerializeError} throw when fail serialize data (include request and response)
 */
export const queryContactsRequest = ({
  basePath = '',
  querystringStringify = querystring,
  username,
  password,
  apiKey,
  accessToken,
  headers,
  credentials,
}: RequestConfiguration = {}) => (): FetchParameters => {
  const queryParameters: UrlQueryInput = {};

  const headerParameters: Headers = new Headers(headers);

  let url: string = `${basePath.replace(/\/+$/, '')}/contacts`;
  if (queryParameters && Object.keys(queryParameters).length > 0) {
    url += '?' + querystringStringify(queryParameters);
  }

  const body = undefined;

  return {
    input: url,
    init: {
      method: 'GET',
      headers: headerParameters,
      credentials,
      body,
    },
  };
};

/**
 * Get contact
 * @throws {APIExceptionError} throw when response is out of 2xx
 * @throws {Response} throw when the response.status is not accepted (fallback throwing)
 * @throws {APISerializeError} throw when fail serialize data (include request and response)
 */
export const queryContactsResponse = (
  configuration: ResponseConfiguration = {},
) => async (response: Response): Promise<Array<Contact>> => {
  if (response.status >= 200 && response.status < 300) {
    return response.json().then(typeSerializer.toValueObjectList('Contact'));
  } else {
    switch (response.status.toString()) {
      case '400': // Bad request.
        throw new APIExceptionError({
          exception: typeSerializer.toValueObject('ApiError')(
            await response.json(),
          ),
          message: `Bad request.`,
          response,
        });
      case '0': // Unknown error.
        throw new APIExceptionError({
          exception: typeSerializer.toValueObject('ApiError')(
            await response.json(),
          ),
          message: `Unknown error.`,
          response,
        });
      default:
        throw response;
    }
  }
};

/**
 * Get contact
 * @throws {APIRequestError} throw when the request is not correct
 * @throws {APIExceptionError} throw when response is out of 2xx
 * @throws {Response} throw when the response.status is not accepted (fallback throwing)
 * @throws {APISerializeError} throw when fail serialize data (include request and response)
 */
export async function queryContacts(
  configuration: OperationConfiguration = {},
): Promise<Array<Contact>> {
  return pipe(
    queryContactsRequest(configuration),
    fetchRequest(configuration),
    queryContactsResponse(configuration),
  )();
}

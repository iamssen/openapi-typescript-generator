/**
 * OpenAPI Petstore
 * This is a sample server Petstore server. For this sample, you can use the api key \"special-key\" to test the authorization filters
 *
 * The version of the OpenAPI document: 1.0.0
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit the class manually.
 */

import type {
  UrlQueryInput,
  OperationConfiguration,
  RequestConfiguration,
  ResponseConfiguration,
  FetchParameters,
} from '@rocket-scripts/openapi';
import {
  pipe,
  collectionFormats,
  querystring,
  fetchRequest,
  APIRequestError,
  APIExceptionError,
} from '@rocket-scripts/openapi';
import { typeSerializer } from '../runtime';

/* eslint-disable */

/**
 * Operation with several unordered 2XX results and one default
 * @throws {APIRequestError} throw when the request is not correct
 * @throws {APISerializeError} throw when fail serialize data (include request and response)
 */
export const withTwoHundredAndDefaultRequest = ({
  basePath = '',
  querystringStringify = querystring,
  username,
  password,
  apiKey,
  accessToken,
  headers,
  credentials,
}: RequestConfiguration = {}) => (): FetchParameters => {
  const queryParameters: UrlQueryInput = {};

  const headerParameters: Headers = new Headers(headers);

  let url: string = `${basePath.replace(
    /\/+$/,
    '',
  )}/tests/withTwoHundredAndDefault`;
  if (queryParameters && Object.keys(queryParameters).length > 0) {
    url += '?' + querystringStringify(queryParameters);
  }

  const body = undefined;

  return {
    input: url,
    init: {
      method: 'GET',
      headers: headerParameters,
      credentials,
      body,
    },
  };
};

/**
 * Operation with several unordered 2XX results and one default
 * @throws {APIExceptionError} throw when response is out of 2xx
 * @throws {Response} throw when the response.status is not accepted (fallback throwing)
 * @throws {APISerializeError} throw when fail serialize data (include request and response)
 */
export const withTwoHundredAndDefaultResponse = (
  configuration: ResponseConfiguration = {},
) => async (response: Response): Promise<string> => {
  if (response.status >= 200 && response.status < 300) {
    return response.text().then(typeSerializer.toValueObject('string'));
  } else {
    switch (response.status.toString()) {
      case '100': // 100 response
        throw new APIExceptionError({
          exception: typeSerializer.toValueObject('number')(
            await response.json(),
          ),
          message: `100 response`,
          response,
        });
      case '400': // 400 response
        throw new APIExceptionError({
          exception: typeSerializer.toValueObject('number')(
            await response.json(),
          ),
          message: `400 response`,
          response,
        });
      case '0': // default response
        throw new APIExceptionError({
          exception: typeSerializer.toValueObject('number')(
            await response.json(),
          ),
          message: `default response`,
          response,
        });
      default:
        throw response;
    }
  }
};

/**
 * Operation with several unordered 2XX results and one default
 * @throws {APIRequestError} throw when the request is not correct
 * @throws {APIExceptionError} throw when response is out of 2xx
 * @throws {Response} throw when the response.status is not accepted (fallback throwing)
 * @throws {APISerializeError} throw when fail serialize data (include request and response)
 */
export async function withTwoHundredAndDefault(
  configuration: OperationConfiguration = {},
): Promise<string> {
  return pipe(
    withTwoHundredAndDefaultRequest(configuration),
    fetchRequest(configuration),
    withTwoHundredAndDefaultResponse(configuration),
  )();
}

/**
 * Operation with several unordered 2XX results and one default
 * @throws {APIRequestError} throw when the request is not correct
 * @throws {APISerializeError} throw when fail serialize data (include request and response)
 */
export const withoutTwoHundredButDefaultRequest = ({
  basePath = '',
  querystringStringify = querystring,
  username,
  password,
  apiKey,
  accessToken,
  headers,
  credentials,
}: RequestConfiguration = {}) => (): FetchParameters => {
  const queryParameters: UrlQueryInput = {};

  const headerParameters: Headers = new Headers(headers);

  let url: string = `${basePath.replace(
    /\/+$/,
    '',
  )}/tests/withoutTwoHundredButDefault`;
  if (queryParameters && Object.keys(queryParameters).length > 0) {
    url += '?' + querystringStringify(queryParameters);
  }

  const body = undefined;

  return {
    input: url,
    init: {
      method: 'GET',
      headers: headerParameters,
      credentials,
      body,
    },
  };
};

/**
 * Operation with several unordered 2XX results and one default
 * @throws {APIExceptionError} throw when response is out of 2xx
 * @throws {Response} throw when the response.status is not accepted (fallback throwing)
 * @throws {APISerializeError} throw when fail serialize data (include request and response)
 */
export const withoutTwoHundredButDefaultResponse = (
  configuration: ResponseConfiguration = {},
) => async (response: Response): Promise<string> => {
  if (response.status >= 200 && response.status < 300) {
    return response.text().then(typeSerializer.toValueObject('string'));
  } else {
    switch (response.status.toString()) {
      case '100': // 100 response
        throw new APIExceptionError({
          exception: typeSerializer.toValueObject('number')(
            await response.json(),
          ),
          message: `100 response`,
          response,
        });
      case '301': // 301 response
        throw new APIExceptionError({
          exception: typeSerializer.toValueObject('number')(
            await response.json(),
          ),
          message: `301 response`,
          response,
        });
      case '0': // default response
        throw new APIExceptionError({
          exception: typeSerializer.toValueObject('string')(
            await response.json(),
          ),
          message: `default response`,
          response,
        });
      default:
        throw response;
    }
  }
};

/**
 * Operation with several unordered 2XX results and one default
 * @throws {APIRequestError} throw when the request is not correct
 * @throws {APIExceptionError} throw when response is out of 2xx
 * @throws {Response} throw when the response.status is not accepted (fallback throwing)
 * @throws {APISerializeError} throw when fail serialize data (include request and response)
 */
export async function withoutTwoHundredButDefault(
  configuration: OperationConfiguration = {},
): Promise<string> {
  return pipe(
    withoutTwoHundredButDefaultRequest(configuration),
    fetchRequest(configuration),
    withoutTwoHundredButDefaultResponse(configuration),
  )();
}
